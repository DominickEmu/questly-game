# Port where the backend API is running (default 8000).
# If you see "proxy error" / ECONNREFUSED when opening the shop, either:
# - Start the backend: cd backend && uvicorn main:app --reload
# - Or if the backend runs on a different port, set it here (e.g. 49293)
# VITE_API_PORT=8000
